# Example env file for backend (placeholders only — safe to commit)
# Copy this file to `.env` and fill real values for local development.

# PostgreSQL connection string (replace placeholders)
DATABASE_URL=postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_NAME>?sslmode=require&channel_binding=require

# JWT secret used to sign tokens
JWT_SECRET=replace-with-a-secure-random-secret

# Port the backend listens on
PORT=5000

# Frontend URL (used for CORS and other integrations)
FRONTEND_URL=http://localhost:3000
